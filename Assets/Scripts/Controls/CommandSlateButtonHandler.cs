using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommandSlateButtonHandler : MonoBehaviour
{
    public CommandSlateButton LastPressedButton { get; private set; }
    public CommandSlateController CommandSlateController { get; private set; }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
